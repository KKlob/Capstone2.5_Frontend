import { createContext } from 'react';

const UserContext = createContext(null);

const MemberContext = createContext(null);

const ThemeContext = createContext(null);

export { UserContext, MemberContext, ThemeContext };